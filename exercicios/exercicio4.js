/* exercicio 4 - tópico 1*/
/* crio a variável n para receber o valor 1 (início da contagem), puxo a variável n pra dentro do laço for e separo com ";" da declaração que compara o valor de n com 100 (n <= 100) e coloco outro argumento que soma 1 com ele mesmo até chegar a 100 (n++), fazendo assim uma contagem de 1 a 100 (utilizo o incremento ++ que significa n = n + o valor de n que é um, se o valor de n fosse diferente de 1,  eu teria que escrever n = n + 1 para que a contagem fosse feita de um por um e não incrementasse a n o valor correspodente a n ) */
var n = 1;
for (n; n <= 100; n++) {
    console.log (n);
}
